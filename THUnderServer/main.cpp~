#include "triserver.h"
#include <iostream>
#include <cstdio>
#include <string>
#include <process.h>
#include <vector>
#include <iostream>
using namespace std;

int main(void) {
    TriServer server(DBPORT, AUDIOPORT, VIDPORT,
                     MAX_CLT_NUM);
    while (true) {
        server.accept();
    }
    return 0;
}
